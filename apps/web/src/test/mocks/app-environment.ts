/** SvelteKit's `$app/environment` for component tests: never the dev overrides. */
export const dev = false;
export const browser = true;
export const building = false;
export const version = "test";
