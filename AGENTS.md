# Repository Agent Instructions

SvelteKit製の個人ブログ兼ポートフォリオ。可読性、アクセシビリティ、Progressive
Enhancement、少ない初期JavaScript、公開コンテンツのGit管理、匿名操作のプライバシーを優先する。

## 作業

- 対象と周辺実装、最寄りの`AGENTS.md`を読み、既存パターンに沿って必要な範囲だけ変更する。
- 未コミット変更を上書きしない。無関係なリファクタリングやlockfile更新をしない。
- 明示的な依頼なしに`git add`、commit、push、rebase、変更の破棄を行わない。
- 生成ファイルは手動編集せず、対応する生成タスクを使う。本番依存の追加は必要性を説明する。
- 関係する場合だけ`docs/architecture.md`、`docs/design-system.md`、
  `docs/content-model.md`、`docs/deployment.md`を読む。
- Acceptedなアーキテクチャと要求が衝突する場合は、回避せず衝突内容を報告する。

## 境界と互換性

- SvelteKitを唯一のデプロイ単位とし、Honoは同じアプリの`/api/v1`へ組み込む。
- 公開コンテンツはGit管理の`.svx`。ディレクトリ、slug、`type:slug`、公開URLは安定識別子。
- 明示的な要求と移行計画なしに、公開API、スキーマ、ストレージ、ルート、デプロイ境界を変更しない。
- `core`はDOM、Deno KV、ネットワーク、`fetch`へ依存させない。依存方向を維持し循環を作らない。
- 外部通信・永続化は明示的なインターフェースを通す。
- Home、記事詳細、フィード、サイトマップ、OGPはプリレンダリングする。
- `/articles`のGET検索はSSRでJavaScriptなしでも利用可能にする。旧Search/Aboutの308転送を維持する。
- Mermaid、Threlte、Three.jsは必要なページでだけ遅延読み込みする。記事の初期依存にHero/WebGLを含めない。
- 不要なハイドレーションやグローバル状態を追加しない。任意機能の失敗で本文や移動を妨げない。
- ルーティング、依存、初期バンドル、レンダリング方式を変える場合は影響を説明する。

## プライバシー

- リアクション・インプレッション・レート制限はDeno KVとrepository interfaceを維持する。
- IP、User-Agent、正確な位置情報、メール、不要な識別子を保存・ログ出力しない。
- same-origin、HTTP境界のZod検証、入力サイズ制限、署名Cookieの
  Secure/HttpOnly/SameSite、actor単位のレート制限、原子的更新を維持する。
- 秘密情報、トークン、本番データ、個人情報をソース・テスト・ログ・文書に追加しない。

## UI

- セマンティックトークンとTailwind CSS 4を使う。Svelteに`<style>`を追加しない。
- 静的な色・寸法・余白をインラインスタイルに書かない。局所的な独自テーマを作らない。
- 人物、植物、動物、手、肖像、ロゴなどのブランド画像をコード生成しない。
- 不足画像を汎用gradient、blob、glass cardで代替しない。技術的・非ブランドの単純SVGは可。
- セマンティックHTML、キーボード、フォーカス、アクセシブルネーム、見出し、コントラスト、 reduced
  motion、forced colors、文字拡大、JavaScriptなしでの利用を維持する。

## 計画と文書

- 文言、軽微なスタイル、明確な単一修正は直接実装する。
- 複数主要ファイル、新操作、読み込み変更、受け入れ条件が不明な作業には短い計画を使う。
- パッケージ境界、公開API、スキーマ、ストレージ、プライバシー、レンダリング・デプロイ境界、
  大規模基盤・ホーム再設計、移行・ロールバックが必要な作業は`.agent/PLANS.md`に従う。
- 現行仕様は`docs/`、導入・コマンドはREADME、作業制約はAGENTSへ置き、過去ログと混在させない。
- 完了・置換済み計画は必要な決定・未確認事項を現行文書へ移し、Git履歴から参照する。

## 検証と報告

変更に直接関係する最小限の確認から始め、影響範囲や失敗に応じて追加する。

- 文言・静的コンテンツ：対象とリンク、必要ならコンテンツ検証。
- スタイル：対象と必要な画面。見た目だけの変更に新しい自動テストは不要。
- ロジック・共通UI：関連テスト、型/Svelteチェック、主要な利用箇所。
- ルーティング・依存・ビルド経路：build、必要ならbudgetとE2E。
- API・データ・セキュリティ：関連テスト、型、バリデーション。

全変更にformat、lint、全テスト、buildを一律で課さない。E2Eは変更した利用者操作と失敗経路へ絞る。
通過済みチェックは、追加変更・失敗・未解決の懸念がない限り繰り返さない。
実装をなぞるだけのテストや、全画面・全端末の組み合わせを機械的に追加しない。
AGENTSには継続的な制約だけを簡潔に置き、公式資料の転載や作業ログを増やさない。
実行していない確認を成功と報告しない。
完了報告は変更内容・対象ファイル・検証結果・重要な未確認事項とリスクを簡潔に示す。
